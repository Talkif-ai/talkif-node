// This file was auto-generated by Fern from our API Definition.

import type * as Talkif from "../index.js";

/**
 * Response after making a call
 *
 * Can be either:
 * - Initiated: Call started immediately (capacity available)
 * - Queued: Call queued for later processing (at capacity/rate limited)
 */
export type MakeCallResponse =
    /**
     * Call initiated immediately (201 Created) */
    | Talkif.MakeCallResponse.Initiated
    /**
     * Call queued for later processing (202 Accepted) */
    | Talkif.MakeCallResponse.Queued;

export namespace MakeCallResponse {
    export interface Initiated {
        type: "initiated";
        /** Unique call identifier */
        callId: string;
        /** Twilio Call SID */
        callSid: string;
        /** Flow used for the AI conversation */
        flowId?: (string | null) | undefined;
        /** Caller phone number */
        fromNumber: string;
        /** Provider used to route the call */
        providerId?: (string | null) | undefined;
        /** Type of telephony provider */
        providerType: Talkif.CallProviderType;
        /** Current call status */
        status: Talkif.CallStatus;
        /** Destination phone number */
        toNumber: string;
    }

    export interface Queued {
        type: "queued";
        /** Estimated wait time before the call is processed */
        estimatedWaitSeconds?: (number | null) | undefined;
        /** Flow ID for the AI conversation */
        flowId: string;
        /** Caller phone number */
        fromNumber: string;
        /** Position in the call queue (1-indexed) */
        position: number;
        /** Queue entry identifier */
        queueId: string;
        /** Destination phone number */
        toNumber: string;
    }
}
