// This file was auto-generated by Fern from our API Definition.

import { TalkifClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ErrorsClient", () => {
    test("error_catalog", async () => {
        const server = mockServerPool.createServer();
        const client = new TalkifClient({ maxRetries: 0, token: "test", environment: server.baseUrl });

        const rawResponseBody = {
            errorCodes: [
                {
                    category: "payment",
                    categoryTitle: "Payment",
                    code: "insufficient_balance",
                    description: "Account balance is too low for this operation — add funds to continue",
                    status: 402,
                    title: "Insufficient Balance",
                },
            ],
            fieldErrorCodes: [
                { code: "invalid_email_format", description: "Email format is invalid (must conform to RFC 5322)" },
            ],
        };

        server.mockEndpoint().get("/api/v1/errors").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.errors.errorCatalog();
        expect(response).toEqual(rawResponseBody);
    });
});
